//
//file: new_expense.dart
//

import 'package:flutter/material.dart';

class NewExpenses extends StatelessWidget {
  const NewExpenses({super.key});
  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [Text('New Expenses file reached')],
    ); //Column
  }
}
